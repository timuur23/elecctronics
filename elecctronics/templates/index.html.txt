<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TechStore 2026 | Electrónica Premium</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #0f1111; color: white; }
        .card { background-color: #1a1a1a; border: 1px solid #333; transition: transform 0.3s; }
        .card:hover { transform: translateY(-5px); border-color: #00d4ff; }
        .card-title { color: #00d4ff; }
        .navbar { border-bottom: 2px solid #00d4ff; }
        .card-img-top { height: 200px; object-fit: cover; }
    </style>
</head>
<body>

<nav class="navbar navbar-dark bg-dark sticky-top">
    <div class="container">
        <a class="navbar-brand fw-bold" href="#">⚡ TECH STORE</a>
        <div class="d-flex align-items-center">
            <span class="badge bg-info text-dark fs-6">🛒 Items: {{ total }}</span>
            <a href="/limpiar" class="btn btn-outline-danger btn-sm ms-3">Limpiar</a>
        </div>
    </div>
</nav>

<div class="container mt-5">
    <h2 class="text-center mb-5">Lo último en Tecnología</h2>
    
    <div class="row">
        {% for p in productos %}
        <div class="col-md-3 mb-4">
            <div class="card h-100 shadow">
                <img src="{{ p.imagen }}" class="card-img-top" alt="{{ p.nombre }}">
                <div class="card-body d-flex flex-column">
                    <span class="badge bg-secondary mb-2" style="width: fit-content;">{{ p.categoria }}</span>
                    <h5 class="card-title">{{ p.nombre }}</h5>
                    <p class="fs-4 fw-bold text-light">${{ p.precio }}</p>
                    <a href="/agregar/{{ p.id }}" class="btn btn-info mt-auto fw-bold">Comprar ahora</a>
                </div>
            </div>
        </div>
        {% endfor %}
    </div>
</div>

<footer class="text-center py-5 mt-5 border-top border-secondary">
    <p class="text-muted">&copy; 2026 TechStore - Desplegado con Flask</p>
</footer>

</body>
</html>